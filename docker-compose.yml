version: '3'
services:
  scrapy:
    build:
      context: ./luxonis
    depends_on:
      - postgres
    networks:
      - mynetwork

  postgres:
    image: postgres:15
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: luxonis_sreality
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345lux
    networks:
      - mynetwork

  web:
    build: ./server
    ports:
      - "8080:8080"
    depends_on:
      - scrapy
    networks:
      - mynetwork

networks:
  mynetwork:
